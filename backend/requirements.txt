# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# 数据库
sqlalchemy>=2.0.36
alembic>=1.13.3
psycopg2-binary>=2.9.10
asyncpg>=0.30.0

# 配置 & 工具
pydantic[email]>=2.9.2
pydantic-settings>=2.6.1
python-dotenv>=1.0.1
email-validator>=2.2.0

# 认证 & 安全
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 不兼容 bcrypt 4.x, 锁住 <4
bcrypt>=3.2.0,<4.0

# HTTP 客户端
httpx>=0.27.2
requests>=2.32.3

# 阿里云
oss2>=2.18.4
# 阿里云 OCR SDK 包名待确认, 阶段 3 需要时再加
# alibabacloud_ocr20210707==2.2.0  # 包名不正确, 暂时注释

# AI (智谱 GLM) — 用最新可用日期版本
zhipuai>=2.1.5

# 任务调度 (后期)
apscheduler>=3.10.4

# 测试
pytest>=8.3.3
pytest-asyncio>=0.24.0
